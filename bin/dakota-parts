#!/bin/bash
set -o errexit -o nounset -o pipefail

build_dir=$1
parts="$build_dir/parts.txt"
shift

if [[ $# -eq 0 ]]; then
  echo $parts
else
  mkdir -p $(dirname $parts)
  cat /dev/null > $parts

  for arg in $@; do
    echo $arg >> $parts
  done
fi
