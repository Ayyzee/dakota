# -*- mode: cmake -*-
cmake_minimum_required (VERSION 3.0)
set (project flintstones)
# dakota-build2project default.project default.build
#set (project-path ${CMAKE_SOURCE_DIR}/default.project)
set (project-path default.project)
set (CMAKE_VERBOSE_MAKEFILE ON)
include (common.cmk)
