# -*- mode: perl -*-
{
  'builddir' => 'dkt',
  'srcs' => [
    '$(srcdir)/dso.$(cc_ext)',
  ],
  'libs' => [
    'dl',
  ],
  'target' => '$(srcdir)/libdso.$(so_ext)',
  'install' => {
    '-rules' => [
    ],
    'files' => [ # install.files
      '$(DESTDIR)$(prefix)/include/dso.$(hh_ext)',
      '$(DESTDIR)$(prefix)/lib/libdso.$(so_ext)',
    ],
  }
}
