# -*- mode: perl -*-
{
  'builddir' => 'dkt',
  'srcs' => [
    '$(srcdir)/dso.$(cc_ext)',
  ],
  'libs' => [
    'dl',
  ],
  'target' => '$(srcdir)/../lib/libdso.$(so_ext)',
  'install' => {
    '-rules' => [
      '$(DESTDIR)$(prefix)/lib/%', '$(srcdir)/../lib/%',
    ],
    'files' => [ # install.files
      '$(DESTDIR)$(prefix)/include/dso.$(hh_ext)',
      '$(DESTDIR)$(prefix)/lib/libdso.$(so_ext)',
    ],
  }
}
