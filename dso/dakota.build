# -*- mode: perl -*-
{
  'builddir' => 'dkt',
  'srcs' => [
    '$(srcdir)/dso.$(cc_ext)',
  ],
  'libs' => [
    'dl',
  ],
  'target' => 'dso',
  'is-lib' => 1,
  'install' => {
    '-rules' => [
    ],
    'files' => [ # install.files
      '$(DESTDIR)$(INSTALL_INCLUDEDIR)/dso.$(h_ext)',
      '$(DESTDIR)$(INSTALL_LIBDIR)/libdso.$(so_ext)',
    ],
  }
}
