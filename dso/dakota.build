# -*- mode: perl -*-
{
  'builddir' => 'build-dkt',
  'srcs' => [
    '${SOURCE_DIR}/dso.${cc_ext}',
  ],
  'libs' => [
    'dl',
  ],
  'target' => 'dso',
  'is-lib' => 1,
  'install' => {
    '-rules' => [
    ],
    'files' => [ # install.files
      '${DESTDIR}${INSTALL_PREFIX}/include/dso.${h_ext}',
      '${DESTDIR}${INSTALL_PREFIX}/lib/libdso.${so_ext}',
    ],
  }
}
