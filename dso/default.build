# -*- mode: perl -*-
{
  'builddir' => 'dkt',
  'srcs' => [
    '$(srcdir)/dso.$(cc_ext)',
  ],
  'libs' => [
    'dl',
  ],
  'target' => 'dso',
  'target.lib?' => 1,
  'install' => {
    '-rules' => [
    ],
    'files' => [ # install.files
      '$(DESTDIR)$(prefix)/include/dso.$(hh_ext)',
      '$(DESTDIR)$(prefix)/lib/libdso.$(so_ext)',
    ],
  }
}
