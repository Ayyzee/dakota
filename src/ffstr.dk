//klass fstr?

klass ffstr
{
  slots str-t;

  // the first two characters are interpreted as the string len (not including NUL)
  // expressed in hex (ff being the largest string 2^16 = 64K).
  // "0bHello World"
}

//klass ffffstr?

// '0' - '9' (48 -  57)
// 'a' - 'f' (97 - 102)
