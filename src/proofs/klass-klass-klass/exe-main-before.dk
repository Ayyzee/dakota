// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

klass str;

klass foo {
  superklass object;
  slots {}
  method init(object-t self) -> object-t {
    self = $init(super);
    return self;
  }
  klass superklass klass;
  klass klass klass;
  klass slots {}
  klass method init(object-t self) -> object-t {
    self = $init(super);
    return self;
  }
}
func main(int-t, str-t[]) -> int-t {
  return 0;
}
