// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

klass str;

klass foo { klass foo-klass;
  superklass object;
  slots {}
  method init(object-t self) -> object-t {
    self = $init(super);
    return self;
  }
} klass foo-klass {
  superklass klass;
  klass klass;
  slots {}
  method initialize(object-t self) -> object-t {
    return self;
} }
func main(int-t, str-t[]) -> int-t {
  return 0;
}
