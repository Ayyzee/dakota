// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

klass k1
{
  method init(object-t self) -> object-t {
    klass = null; // error
    self = $init(super);
    return self;
  }
}

klass k2
{
  superklass k1;

  method init(object-t self) -> object-t {
    self = $init(super);
    return self;
  }
}

int-t main() {
  make(k2::klass);
  return 0;
}
