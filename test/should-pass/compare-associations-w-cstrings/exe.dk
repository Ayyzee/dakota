// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

#include <assert.h>

int-t main()
{
  object-t pair1 = make(pair::klass,
                        key : #"key0",
                        element : #"element0");

  object-t pair2 = make(pair::klass,
                        key : #"key0",
                        element : #"element0");

  intptr-t result = dk::compare(pair1, pair2);
  assert(0 == result);

//   object-t perl-object-output-stream = make(perl-object-output-stream::klass);
//   dk::write(perl-object-output-stream, pair);
//   dk::close(perl-object-output-stream);

  return 0;
}
