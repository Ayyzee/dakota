// -*- mode: C++; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

int-t main()
{
  uint32-t hash-value = dk-hash($default-initial-capacity);
  switch(hash-value)
  {
    case $'default-initial-capacity':
      break;
    default:
      fprintf(stderr, "%x\n", hash-value);
      abort();
  }
  return 0;
}
