// -*- mode: C++; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

#define CONCAT(a, b) a ## b
#define STRINGIFY(a) #a

int-t main()
{
  const char8-t* foobar = "foobar";
  printf("%s\n", CONCAT(foo, bar));
  printf("%s\n", STRINGIFY(foo));

  return 0;
}
