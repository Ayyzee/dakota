// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

#include <assert.h>

klass foobar;

int-t main() {
  object-t o = make(foobar::klass);
  $foo(o);
  $bar(o);
  return 0;
}
