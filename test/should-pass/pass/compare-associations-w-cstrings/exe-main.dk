// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

#include <assert.h>

klass pair;

int-t main()
{
  object-t pair1 = make(pair::klass,
                        first : #"key0",
                        last :  #"element0");

  object-t pair2 = make(pair::klass,
                        first : #"key0",
                        last :  #"element0");

  intptr-t result = $compare(pair1, pair2);
  assert(0 == result);

//   object-t json-object-output-stream = make(json-object-output-stream::klass);
//   $write(json-object-output-stream, pair);
//   $close(json-object-output-stream);

  return 0;
}
