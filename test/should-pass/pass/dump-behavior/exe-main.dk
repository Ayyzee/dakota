// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

//klass bit-vector;
klass sorted-set;

int-t main()
{
//   object-t ss-behavior = $behavior(sorted-set::klass);
//   $dump(ss-behavior);
//   $dump-behavior(sorted-set::klass);
  $dump-methods(sorted-set::klass);

//   object-t ss-behavior-copy = $copy-shallow(ss-behavior);
//   $op-not(ss-behavior-copy);
//   $dump(ss-behavior-copy);

//   object-t hs-behavior = $behavior(hashed-set::klass);
//   $dump(hs-behavior);
//   $dump-behavior(hashed-set::klass);
//   $dump-methods(hashed-set::klass);

//   object-t hs-behavior-copy = $copy-shallow(hs-behavior);
//   $op-not(hs-behavior-copy);
//   $dump(hs-behavior-copy);

//   boole-t s-sh = $behavior?(sorted-set::klass, hashed-set::klass);
//   fprintf(stdout, "%i = $behavior?(sorted-set::klass, hashed-set::klass)\n", s-sh);

//   boole-t s-hs = $behavior?(hashed-set::klass, sorted-set::klass);
//   fprintf(stdout, "%i = $behavior?(hashed-set::klass, sorted-set::klass)\n", s-hs);

//   object-t st-behavior = $behavior(sorted-table::klass);
//   $dump(st-behavior);

//   object-t ht-behavior = $behavior(hashed-table::klass);
//   $dump(ht-behavior);

//   boole-t t-sh = $behavior?(sorted-table::klass, hashed-table::klass);
//   fprintf(stdout, "%i = $behavior?(sorted-table::klass, hashed-table::klass)\n", t-sh);

//   boole-t t-hs = $behavior?(hashed-table::klass, sorted-table::klass);
//   fprintf(stdout, "%i = $behavior?(hashed-table::klass, sorted-table::klass)\n", t-hs);

  return 0;
}
