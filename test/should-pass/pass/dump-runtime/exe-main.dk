// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

int-t main() {
  $dump(object::klass);
  //$dump-methods(object::klass);
  printf("---\n");
  $dump(klass::klass);
  //$dump-methods(klass::klass);
  printf("---\n");
  if (null) {
    $dump(null->klass);
    //$dump-methods(null->klass);
    printf("---\n");
    $dump(null);
  } else {
    fprintf(stderr, "nullptr\n");
    printf("---\n");
    fprintf(stderr, "nullptr\n");
  }
  return 0;
}
