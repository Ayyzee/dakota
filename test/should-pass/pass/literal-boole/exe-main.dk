// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

static object-t f = #false;
static object-t t = #true;

int-t main() {
  $dump(f);
  $dump(#false);
  $dump(t);
  $dump(#true);
  return 0;
}
