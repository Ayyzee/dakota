// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

static object-t b1 = #false;
static object-t b2 = #true;

int-t main() {
  $dump(b1);
  $dump(#false);
  $dump(b2);
  $dump(#true);
  return 0;
}
