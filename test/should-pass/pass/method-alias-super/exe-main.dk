// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

#include <assert.h>

klass k1
{
  method [[alias(hash)]] uintmax-t alias-hash(object-t);
}

int-t main()
{
  selector-t hash-selector = SELECTOR(hash(object-t));
  method-t hash = $method-for-selector(k1::klass, hash-selector);
  selector-t alias-hash-selector = SELECTOR(alias-hash(object-t));
  method-t alias-hash = $method-for-selector(k1::klass, alias-hash-selector);
  assert(hash == alias-hash);
  dkt-dump-methods(k1::klass);
  return 0;
}
