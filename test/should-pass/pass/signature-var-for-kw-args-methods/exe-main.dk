// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

klass k1 {
  method m1(object-t self, uint32-t depth, uint32-t width :, uint32-t length : 5) -> object-t {
    USE(depth);
    USE(width);
    USE(length);
    printf("%s(%s)\n", __method__->name, __method__->parameter-types);
    return self;
  }
}
int-t main() {
  object-t o = make(k1::klass);
  $m1(o, 0, #width : 0, #length : 3);
  return 0;
}
