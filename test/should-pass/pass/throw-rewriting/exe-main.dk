// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

int-t main()
{
  try {
    throw (object-t)~0;     // does not rewrite correctly
    //throw cast(int-t)42; // rewrites correctly
  } catch (...) {

  }
  return 0;
}
