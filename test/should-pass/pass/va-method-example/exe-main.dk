// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

klass fred {
  method init(object-t self) -> object-t {
    return self;
  }
  method va::wilma(object-t self, va-list-t args) -> object-t {
    USE(args);
    printf("%s()\n", __method__->name);
    return self;
  }
}
int-t main() {
  object-t o = make(fred::klass);
  selector-t s = SELECTOR(va::wilma(object-t, va-list-t));
  method-t m = $method-for-selector(klass-of(o), s);
  USE(m);
//   va-method-t vm = $va-method-for-selector(klass-of(o));
  return 0;
}
