// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

klass vector;
trait stack;

int-t main() {
  int-t value = 0;
  object-t stack = make(vector::klass);
  $push(stack, null);
  object-t object = $pop(stack);
  USE(object);
  // hackhack: should test for null
  return value;
}
