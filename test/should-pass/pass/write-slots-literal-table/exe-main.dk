// -*- mode: Dakota; c-basic-offset: 2; tab-width: 2; indent-tabs-mode: nil -*-

#define DEFAULT-TABLE-KLASS sorted-table::klass

klass int32; // hackhack
klass pair; // hackhack
klass json-object-output-stream;
klass sorted-table; // hackhack

int-t main()
{
  int32-t a = 11, b = 13, c = 17, d = 19;

  object-t o = #{ #a : b, #c : d };

  dk::dump(o);

  object-t json-object-output-stream = make(json-object-output-stream::klass);
  dk::write-slots(o, json-object-output-stream);
  dk::close(json-object-output-stream);

  USE(a);
  USE(c);
  return 0;
}
